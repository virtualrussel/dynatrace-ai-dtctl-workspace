{
  "_comment": "Mirrors .mcp.json (VS Code format). After editing, sync to Claude Code: jq '{\"mcpServers\": .servers}' .vscode/mcp.json > .mcp.json",
  "servers": {
    "dynatrace-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@dynatrace-oss/dynatrace-mcp-server@latest",
        "--stdio"
      ],
      "env": {
        "DT_ENVIRONMENT": "https://YOUR_TENANT_ID.apps.dynatrace.com"
      }
    }
  }
}
